点は，数値のリスト[a,b]と考えることができます。
リストの名前をAとするとき，A_n(nは整数値)とすることで，Aのn番目の要素にアクセスできます。
点A=[a,b]と分かっていれば，Aのx座標はリストの1番目の要素 A_1 で，その値はaです。
同様にAのy座標は2番目の要素 A_2 で，その値はbです。
通常は，Aのx座標をA.xとし，y座標をA.yとします。
A.xy とすることで点Aの座標を利用しようとしていることがコードから読み取れます。